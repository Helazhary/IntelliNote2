# SmartNotes AI — frontend environment (see docs/ENV_SETUP.md)
# Only NEXT_PUBLIC_* vars are exposed to the browser. NEVER put secrets here (NFR-SEC-04).

# Backend API base URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
